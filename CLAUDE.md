# Claude Code repository pointer

Read and follow [AGENTS.md](AGENTS.md) before any repository action. It is the single source of truth for language, workspace boundaries, workflow, Git policy, UI-0 limits, and safety constraints.

For MEGIS construction, also read the current state in `execution/PROJECT_STATE.md`, the task graph in `execution/WORK_QUEUE.yaml`, blockers, `execution/AGENT_CLAIM.json`, and the latest handoff/review before claiming work. Use the V3 blueprint referenced by `README.md`; treat older blueprints as archived baselines.

