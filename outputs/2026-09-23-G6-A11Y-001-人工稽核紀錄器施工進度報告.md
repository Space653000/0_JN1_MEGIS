# G6-A11Y-001 人工稽核紀錄器施工進度報告

## 本段結論

本段完成真人鍵盤與螢幕閱讀器抽查的安全資料收集工具，但沒有冒充真人執行抽查。`G6-A11Y-001` 仍為 `in_progress`，`closureEligible` 仍為 `false`；控制面維持 62 done、1 in progress、11 planned。

## 施工內容

- 新增 `scripts/record_g6_a11y_manual.py`，互動收集 reviewer、作業系統、瀏覽器、螢幕閱讀器與版本。
- 強制操作者逐字輸入 `YES` 接受 `I personally performed this manual accessibility audit.`，Agent 不得代簽。
- 分開保存 keyboard 與 screenReader 各 6 項檢查，避免同名 `design-form` 混淆，共 12 項。
- 結果只允許 `passed` 或 `failed`；`failed` 必須附人工觀察備註。
- 輸出路徑必須位於 `C:\0_JN1_MEGIS`，使用 write-once 模式，既有檔案一律拒絕覆寫。
- 寫入前沿用 `evaluate_document` 完成 schema、canonical order、狀態一致性與完整性檢查。
- 新增 10 項 recorder tests，並納入 `scripts/verify_g6_a11y_001.mjs` 與正式證據 JSON。

## 嚴格比較

| 面向 | 本段前 | 本段後 | 邊界 |
|---|---|---|---|
| 人工紀錄方式 | 手動複製及編輯 JSON | 互動式、固定 12 項、write-once | 仍須真人實際操作 |
| 身分聲明 | schema 可驗證 | 收集時另要求逐字 `YES` | 不驗證法定身分 |
| 結果語彙 | verifier 事後檢查 | 輸入時與寫入前雙重限制 | failed 不等於可閉合 |
| 歷史證據 | 可能被手動覆寫 | 工具拒絕覆寫 | 人工仍可繞過工具，審查需看 Git history |
| 工作區隔離 | verifier 限制輸入／輸出 | recorder 也限制輸出 | 未觸碰其他兩個專案 |
| G6 閉合 | 不符合 | 仍不符合 | 尚缺具名真人稽核紀錄 |

## 驗證盤點

- Recorder tests：10 passed。
- G6 verifier：automated passed；manual keyboard pending；manual screen reader pending；closure ineligible。
- 完整 baseline：607 Python tests、22 frontend tests、control-plane、secret scan、artifact policy、toolchain、maturity、lint、typecheck、production build 全綠。
- 人工證據：未建立，避免將 Agent、自動化瀏覽器或工具測試偽裝為真人抽查。
- 工程／release 製品：未生成。
- 外部服務與其他專案：未呼叫、未修改、未共用。

## 真人下一步

實際執行鍵盤與螢幕閱讀器抽查的人員，應在本倉庫執行：

```powershell
.\.venv\Scripts\python.exe scripts\record_g6_a11y_manual.py --output artifacts\g6-a11y-001\manual-audit.json
.\.venv\Scripts\python.exe scripts\verify_g6_a11y_manual.py --require-complete --input artifacts\g6-a11y-001\manual-audit.json --output artifacts\g6-a11y-001\manual-audit-verification.json
```

若任一檢查失敗，保留該紀錄、修正問題後以新檔名重測；不得覆寫既有證據。只有 12 項均通過且完整 baseline 維持全綠，才可評估閉合 G6-A11Y-001。
