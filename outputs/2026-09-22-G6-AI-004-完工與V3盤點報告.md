# G6-AI-004 完工與 MEGIS V3.0 施工盤點報告

> 文件治理
> - 目的：記錄 grounded explanation 的來源限制、逐數值追溯、負向驗證與同步狀態。
> - 目前內容：schema、hash binding、JSON Pointer、fail-closed tests、V3 工作圖與下一步。
> - Owner：MEGIS Builder；使用者保有否決權。
> - 最後審查 commit：`d27d16e17ed81eb0fb8ac4e8b0be83891df14d82`。

## 結論

`G6-AI-004 — AI grounded explanation` 已達 E3。Provider output 只能是 schema-bound 的繁體中文段落與逐數值 citations；唯一允許的事實來源為 Engineering IR、rule result、package manifest。核心自行重算來源 canonical SHA-256，並把每個文字數值 token 對回 citation 的 source ID、JSON Pointer 與實際數值。

驗證採 fail closed：任一數值漏引或多引、文字與來源數值不符、pointer 不存在或指向非數值、來源被竄改、hash 偽造、來源集合不完整、來源類型越權，整份結果都回 `MEGIS-AI-002`，不自動修補、不部分採用。因此 example evidence 的 4/4 數值可追溯，Explanation grounding 為 100%。

## 藍圖驗收比較

| 要求 | 實作與證據 | 結果 |
|---|---|---|
| IR、規則結果、manifest 為唯一事實來源 | `ALLOWED_SOURCE_TYPES` 封閉三類，非法來源拒收 | passed |
| 每個數值可在來源中找到 | 每次 token 都需一筆 citation，pointer 解析值必須數值相等 | passed |
| 找不到即拒絕輸出 | 全部錯誤以 `MEGIS-AI-002`、`accepted: false` fail closed | passed |
| 防止引用過期來源 | source fingerprint 集合完全一致，核心自行重算 | passed |
| Explanation grounding 100% | E3 evidence 4/4；測試涵蓋重複數值逐次引用 | passed |
| 不改寫工程事實 | 模組沒有 IR／rule／manifest／geometry 寫入 API | passed |
| CI 不呼叫真實 provider | recorded JSON only | passed |

## 驗證結果

| 驗證 | 結果 |
|---|---|
| G6-AI-004 tests | 15 passed |
| G6-AI-001～004 regression | 62 passed |
| E3 verifier | 8/8 checks passed |
| Grounding example | 4/4 numbers，IR／rule result／manifest 三類皆覆蓋 |
| 全庫 Python | 585 passed |
| Frontend | 12 passed／3 files |
| lint／typecheck／production build | passed |
| Control plane／secret／artifact policy | passed |
| 外部 provider／工程 artifact／release artifact | 0／0／0 |

完整 CI 初次執行由文件治理閘門攔下新說明文件缺少四個治理欄位；補上 purpose、current content、owner、last reviewed commit 後，控制面與完整 CI 全綠。此紀錄保留以證明文件沒有繞過 repository 的可追溯政策。

## 進度與同步

| 項目 | 施工前 | 本段閉合後 |
|---|---:|---:|
| done | 61 | 62 |
| in progress | 1 | 1 |
| planned | 12 | 11 |
| current work item | G6-AI-004 | G6-A11Y-001 |

功能 commit `d27d16e17ed81eb0fb8ac4e8b0be83891df14d82` 已推送至 `origin/codex/g1-req-001-research`，本機與遠端 SHA 相同，未使用 force push。控制面封板 commit 會在本報告與 `/progress` 驗證後同步。

## 邊界盤點

- 全部寫入、測試暫存、cache 與 build 均位於 `C:\0_JN1_MEGIS`。
- 未修改、未共用、未連結另外兩個施工中資料夾。
- 未呼叫外部 AI provider，未生成工程或 release artifact。
- UI-0 的 `Synthetic demo data` 與 `No engineering artifact generated` 標示不變。
- 100% grounding 只代表數值忠實於指定來源，不代表來源本身已獲工程簽核。

## 下一步

`G6-A11Y-001` 需執行 WCAG 2.2 AA 自動化檢查，以及鍵盤與螢幕閱讀器人工抽查。自動化結果不能代替人工證據；若本輪無法取得真實人工抽查，必須保留為明確未完成項，不得以模擬結果閉合。
