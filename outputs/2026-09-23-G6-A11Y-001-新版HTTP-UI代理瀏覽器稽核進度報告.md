# G6-A11Y-001 新版 HTTP UI 代理瀏覽器稽核進度報告

## 結論

本段已在實際 Chrome、`http://127.0.0.1:4173` 與 loopback API `http://127.0.0.1:4174` 上完成新版 UI 的代理鍵盤走查。7 項實際執行情境通過；行動版瀏覽器情境沒有執行，明確記為 `not_run`。這份證據不是具名真人鍵盤或螢幕閱讀器驗收，因此 `G6-A11Y-001` 仍為 `in_progress`，不能解鎖 `G6-USE-001`。

## 本段盤點

| 情境 | 操作與觀察 | 結果 |
|---|---|---|
| `/progress` 驗收 disclosure | Tab 聚焦「查看驗收條件」，Enter 後 `aria-expanded` 轉為 expanded，顯示 pending criterion、支援證據與驗證指令 | passed |
| 桌面導覽與 route focus | Shift+Tab 到「設計工作台」，Enter 進入 `/design`，焦點落在 `main#main-content` | passed |
| `/design` 鍵盤順序 | Tab 依序到寬、深、高、3 個元件選項、用途、數量、優先目標、PCB 狀態與送出按鈕 | passed |
| 表單 accessible name | AX tree 可辨識每個輸入、選擇與送出動作的名稱 | passed |
| `/review` DRAFT 邊界 | Tab 到確認 checkbox，Space 勾選後「檢視 IR 結果」由 disabled 轉 enabled | passed |
| 真實 API 草稿流程 | Enter 經 `/review`、`/run` 抵達 `/results`；顯示 DRAFT 邊界、correlation ID、response SHA-256、4 components、2 unknowns | passed |
| skip link | 鍵盤到「跳至主要內容」，Enter 後 URL 為 `#main-content` 且焦點落在 main | passed |
| mobile navigation | 本輪未模擬 mobile viewport；只有自動化 contract coverage | not_run |

## 與舊代理稽核比較

| 面向 | 2026-09-22 舊 UI 稽核 | 2026-09-23 新 HTTP UI 稽核 |
|---|---|---|
| 資料流 | 當時 UI 狀態與語意 | 真實 React → 127.0.0.1 API → versioned DRAFT IR |
| 鍵盤深度 | disclosure、route focus、語意與 layout | 從 progress 一路操作至 results，包含 checkbox 解鎖與表單完整焦點序列 |
| 工程界線 | 驗證 UI 邊界 | 直接觀察 `DRAFT IR only` 與 `No engineering artifact generated` |
| 人工效力 | 非真人證據 | 仍非真人證據 |
| 螢幕閱讀器 | 未執行 | 未執行 |

## 證據與邊界

- 結構化證據：`artifacts/g6-a11y-001/browser-http-ui-audit.json`
- UI 基線：`26f4e48e73d9196af4eda2df66c8c501c0b48777`
- `humanEvidence: false`
- `screenReaderUsed: false`
- `manual.keyboard: pending`
- `manual.screenReader: pending`
- `closureEligible: false`
- 未產生 STEP、工程圖、BOM release 或其他工程製品。
- 未接觸、修改、共用或依賴 `C:\0_JN1_AERIS` 與 `C:\0_JN1_Offline-Local-Voice-Agent`。

## V3.0 藍圖目前狀態

- 工作圖：63 done／1 in progress／11 planned，共 75 項。
- 唯一在製：`G6-A11Y-001`。
- G6 狀態：active，尚未 acceptance。
- 待補必要證據：由實際人員完成具名鍵盤抽查，以及使用 NVDA、JAWS、Narrator 或 VoiceOver 之一完成具名螢幕閱讀器抽查，再通過 `scripts/verify_g6_a11y_manual.py --require-complete`。
- 在上述真人證據完成前，不得把 `G6-A11Y-001` 設為 done，也不得開始依賴它的 `G6-USE-001`。
