# G6-A11Y-001 真人驗收 Blocker 可視化進度報告

## 結論

本段把目前唯一無法由 Agent 合法完成的缺口正式登錄為 `B-G6-A11Y-HUMAN-001`，並在 `http://127.0.0.1:4173/progress` 顯示 blocker 標題、責任者與解除條件。這不是把工項虛報為完成，而是讓藍圖依賴、人工責任和下一步行動在控制面及網頁保持一致。

## 已完成

- `execution/BLOCKERS.yaml` 與 `execution/WORK_QUEUE.yaml` 使用相同 blocker 清單。
- `G6-A11Y-001.blockedBy` 指向 `B-G6-A11Y-HUMAN-001`。
- `/progress` 顯示「等待真人驗收」。
- 顯示責任者：`實際無障礙抽查者（具名）`。
- 顯示解除方式：使用 write-once 紀錄器完成 6 個鍵盤與 6 個螢幕閱讀器檢查。
- 新增 UI regression test，防止 blocker 在後續修改中消失。

## 為何仍維持 in_progress

控制面 1.1 契約要求恰有一個 `in_progress` 工項，且它必須等於 `currentWorkItem`；因此沒有把目前工作項改成 `blocked`。`blockedBy` 才是此處的真實等待條件。這保留單一 WIP 規則，同時避免把等待真人的狀態隱藏在一般施工文字中。

## V3.0 依賴影響

| 工項 | 狀態 | 不能前進的原因 |
|---|---|---|
| G6-A11Y-001 | in_progress | 等待具名真人鍵盤及螢幕閱讀器簽錄 |
| G6-USE-001 | planned | 直接依賴 G6-A11Y-001 done |
| G6-E2E-001 | planned | 依賴 G6-USE-001 done |
| G6-REV-001 | planned | 依賴 G6-E2E-001 done |
| G6-ACC-001 | planned | 依賴 G6-REV-001 done |
| G7／G8 | planned | 依賴 G6 acceptance |

## 真人解除流程

```powershell
.\.venv\Scripts\python.exe scripts\record_g6_a11y_manual.py --output artifacts\g6-a11y-001\manual-audit.json
.\.venv\Scripts\python.exe scripts\verify_g6_a11y_manual.py --require-complete --input artifacts\g6-a11y-001\manual-audit.json --output artifacts\g6-a11y-001\manual-audit-verification.json
```

紀錄者必須實際完成檢查、具名、填入瀏覽器／作業系統／輔助技術版本並接受 attestation。Agent 不得代填、推測或將 AX tree 代理稽核當成真人螢幕閱讀器證據。

## 邊界

- 工作圖仍為 63 done／1 in progress／11 planned。
- `closureEligible: false`。
- 沒有啟動 G6-USE-001。
- 沒有產生 STEP、圖面或 release package。
- 未接觸 `C:\0_JN1_AERIS` 或 `C:\0_JN1_Offline-Local-Voice-Agent`。
