# 2026-09-19 G1-ENV-001 完工進度報告

> 一行摘要：`G1-ENV-001 機器可讀 envelope` 完工並標 done（E3）；建立 `config/envelope/envelope.yaml`＋`schemas/v3/envelope.schema.json`＋`megis/envelope/` loader/guard，與 `SUPPORTED_ENVELOPE.md` 一致性由測試把守，超出已驗證範圍即回 `MEGIS-ENV-001` 且禁止 silent clamp；CI 於 `4a2d9e2` fresh-checkout 全綠。

## 1. 本段施工內容

| 項目 | 結果 |
|---|---|
| Work item | `G1-ENV-001 — 機器可讀 envelope`（planned → done） |
| 藍圖依據 | v3 §2.4（envelope）、§5.1（SUPPORTED_ENVELOPE.md）、invariant 18、§9.1 承接 G1-REQ-001 |
| 實作 commit | `4a2d9e281f9fbf57a54122e546b7814ff78768b0` |
| 機器設定來源 | `config/envelope/envelope.yaml`（schemaVersion 1.0.0、envelope_id `legacy-reference-fixture`） |
| Schema | `schemas/v3/envelope.schema.json`（2020-12；`silent_clamp` enum 僅 `false`、`error_codes` pattern `MEGIS-[A-Z]{2,4}-[0-9]{3}`） |
| 模組 | `megis/envelope/envelope.py`＋`__init__.py`（`Envelope`、`load_envelope`、`check_within_envelope`） |
| 文件 | `docs/SUPPORTED_ENVELOPE.md`（與機器檔一致性測試）＋`config/envelope/README.md` |
| 測試 | `tests/test_g1_env_001.py` 7 筆（schema 載入、機器↔文件一致性、邊界精確在內、超出各軸逃逸、schema 正負向、檔案存在） |
| 驗證檔 | `artifacts/g1-env-001/verification.json` |
| 本機重跑 | full baseline passed（126 Python、8 artifact policy、242 secret-scan 0 發現、244 artifact-policy、9 frontend、build） |
| 獨立 CI | run `35432809248` at `4a2d9e2`，success，139 秒 |

## 2. 施工前後比較

| 面向 | 施工前 | 施工後 |
|---|---|---|
| 機器可讀 envelope | 不存在 | `config/envelope/envelope.yaml`＋schema＋loader/guard |
| 單一機器設定來源 | 無（範圍散落在文件/schema 假設） | `envelope.yaml` 為唯一機器來源，禁止各處硬編碼 |
| 文件↔設定一致性 | 無自動把守 | `test_machine_envelope_matches_human_doc` 逐項比對 |
| 超出範圍行為 | 未定義（可能靜默收斂） | 拋 `MEGIS-ENV-001`，schema 禁止 `silent_clamp` |
| 已驗證 legacy 範圍 | 分散敘述 | `120×80×20 mm`、AL6061、3-axis CNC、cover×1、fastener×4、PCB envelope、USB-C cutout 全部入機器檔 |
| V3 目標範圍 | 無正式標記 | `v3_target.verified: false`，保留給 `G1-REQ-001` 研究核准 |
| full pytest | 119 | 126（+7） |

## 3. 已建立的 envelope 結論

- 已驗證 legacy Reference Fixture envelope：外型 `120 × 80 × 20 mm`、minimum wall `2 mm`、material `AL6061`、machining `3-axis CNC`、1 cover、4 fasteners、PCB envelope present、USB-C cutout present。
- PCB 與 USB-C 現有數值是隔離的 V2 過渡假設，`pcb_envelope_verified: false`，不是研究核准值。
- V3 目標：外形 `120 × 80 × 35 mm`、兩片 PCB、USB-C、M3 removable cover；`v3_target.verified: false`，需 `G1-REQ-001` 公開來源研究核准後才視為已驗證 envelope。
- 超出已驗證範圍一律以 `MEGIS-ENV-001` 明確拒絕，不允許 silent clamp（invariant 18）。

## 4. V3.0 進度盤點（74 項）

| 狀態 | 數量 | 百分比 |
|---|---:|---:|
| done | 27 | 36.5% |
| in_progress | 1 | 1.4% |
| planned | 46 | 62.2% |
| total | 74 | 100% |

### 4.1 各施工線

| 範圍 | done | in_progress | planned | 說明 |
|---|---:|---:|---:|---|
| V3C（G2 gate 內） | 7 | 1 | 1 | REV 保持 in_progress；ACC 待承接閉合 |
| G0 | 11 | 0 | 0 | 全部完成 |
| G1 | 6 | 0 | 3 | ENV 完成；承接 REQ/REV/ACC |
| G2 engineering | 3 | 0 | 4 | G2-CAD-004 起 |
| G3 | 0 | 0 | 8 | 規則與驗證 |
| G4 | 0 | 0 | 6 | 模組與組合 |
| G5 | 0 | 0 | 6 | 套件與可重現 |
| G6 | 0 | 0 | 11 | UI/AI/A11y/E2E |
| G7 | 0 | 0 | 4 | 聲學 |
| G8 | 0 | 0 | 3 | 機器人 |

備註：74 項工作圖之中，V3C 9 項（皆掛在 G2 gate 內、含 REV/ACC）＋G0 11 項＋G1 9 項＋G2 engineering 7 項＋G3–G8 38 項 = 74；上表合計 done 27、in_progress 1、planned 46。UI-0A~0D 是已驗收 Gate（體驗原型，非工程能力），不計入 74 項。

## 5. 同步狀態

| commit | 內容 | push |
|---|---|---|
| `4a2d9e2` | G1-ENV-001 實作（envelope.yaml、schema、loader/guard、一致性測試） | 已同步 |
| 本報告後續 commit | queue/state/verification/LAS 盤點＋本報告 | 本段同步 |

## 6. 未完成與下一步

- `V3C-REV-001` 維持 `in_progress`：G1 承接項全 done 後才能將 REV/ACC 標 done。
- 下一步：`G1-REQ-001`（參考案例參數研究與決定，v3 §9.1：公開來源研究、Reference Fixture 無未解決 critical unknown）→ `G1-REV-001` → `G1-ACC-001`，最後 `V3C-ACC-001` 簽核後回 `G2-CAD-004`。
- 殘留風險：具名工程師未設置（上限 `PROTOTYPE`）、Design Run 90 天 cleanup command 未實作、CI artifact upload 未實作；均已有承接追蹤。V3 envelope 目標數值未核准前不得作為工程輸出。
