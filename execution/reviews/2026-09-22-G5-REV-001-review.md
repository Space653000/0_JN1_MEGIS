# 審查報告 — G5-REV-001 @ G5 Gate

- 審查 Agent：codex
- 審查 session ID：codex-fresh-review-g5-2026-09-22
- 施工 Agent：codex
- 施工 session ID：codex-builder-g5-2026-09-22（G5-PKG-001／BOM-001／DRW-001／REP-001 各工項分 session 施工）
- 乾淨 checkout 方式：於工作區內 `.runs/g5-rev-001-clean`（git-ignored）以 `git clone` 固定 commit `51259f6` 重跑
- 審查時間：2026-09-22T18:40:00+08:00
- 環境：Windows（本機），Python 3.11.9，CadQuery 2.8.0，OpenCascade 7.9.3.1，Node 24.17.0
- 結論：passed

## 驗收條件對照

| # | 條件 | 證據 | 等級 | 重跑結果 | 判定 |
|---|---|---|---|---|---|
| 1 | 全新 session 在乾淨 checkout 重跑 G5 全部驗證並出具 passed 報告 | 本報告 + 乾淨 clone `tests/test_g5_*.py` 77 tests passed + `verify_g5_rep_001.py` allChecksPassed + control-plane verifier passed | E3 | PASSED | 合格 |
| 2 | G5-REV-001 依賴（G5-REP-001）done，且 G5 其餘工項（PKG-001／BOM-001／DRW-001／REP-001）皆 done | WORK_QUEUE：`G5-PKG-001=done`、`G5-BOM-001=done`、`G5-DRW-001=done`、`G5-REP-001=done`，dependsOn 鏈完整 | E3 | PASSED | 合格 |

## 重跑紀錄（乾淨 checkout @ 51259f6）

```text
node scripts/verify-control-plane.mjs
Control plane valid: 74 work items, 14 gates, current G5-REV-001, 0 migration deferrals
```

```text
.venv/Scripts/python.exe -m pytest tests/test_g5_pkg_001.py tests/test_g5_bom_001.py tests/test_g5_drw_001.py tests/test_g5_rep_001.py
collected 77 items
77 passed in 6.16s
```

前置條件：乾淨 clone 首次重跑前需先建立 `.temp/pytest`（git-ignored 環境前置條件），否則 pytest `--basetemp=.temp/pytest` 無法建立 parent directory；這與 G2/G3/G4-REV-001 紀錄一致，非 code 變更需求。

```text
.venv/Scripts/python.exe scripts/verify_g5_rep_001.py -> allChecksPassed: true
（source／clean／golden reportFingerprint 全等：316e6ed5…58；g0-cad 三格式語意指紋 mismatch 0）
```

## G5 驗證摘要（乾淨 clone）

| 量測 | 結果 |
|---|---|
| G5 單元測試 | 77/77 passed（pkg 25、bom 25、drw 21、rep 6） |
| G5-REP-001 E3 驗證腳本 | 1/1 allChecksPassed（5 checks） |
| PKG-001 | 25 tests；manifest-corpus 18 cases（7 positive／11 negative） |
| BOM-001 | 25 tests；bom-corpus 12 cases（5 positive／7 negative） |
| DRW-001 | 21 tests；drawing-corpus 12 cases（5 positive／7 negative）；確定性 SVG＋水印＋QA checklist |
| REP-001 | 6 tests；frozen golden 語意指紋對齊；乾淨重建 reportFingerprint 三路全等 |
| 主樹全量 | G5-REP-001 閉合時 baseline CI：498 Python + 9 Frontend 全綠；secret scan 417 tracked / 0 finding；artifact policy 420 tracked / 8 negative tests 通過 |

## 確認的 G5 能力鏈

- G5-PKG-001：prototype package manifest（L1 byte SHA-256 + L2 semantic fingerprint），六種 fingerprint kind（step／stl／dxf／json／text／binary）；`megis/package/manifest.py`＋`verify.py`，18 cases。
- G5-BOM-001：確定性 BOM exporter（十二欄、item 升冪、UTF-8／LF CSV），數量讀 IR component quantity（不臆測），module 由 provenance marker 追溯；`megis/package/bom.py`，12 cases。
- G5-DRW-001：fixed-template draft drawing（IR metadata 驅動、固定 `fixture-a4-landscape@1.0.0`、dimension whitelist、DRAFT 水印、QA checklist 寫入 manifest）；確定性 SVG 無時間戳；`megis/package/drawing.py`，12 cases。
- G5-REP-001：clean-environment reproducibility test；semantic fingerprint 為可重現單位，`bom.csv`／`draft_drawing.svg`／`reference_case.stl` byte-穩定，STEP/DXF exporter metadata delta 有紀錄；`megis/package/repro.py`＋`contracts/g5/golden/repro-fingerprints.json`（corpusId `g5-repro-fingerprints@1.0.0`）；任何語意指紋漂移 raise `MEGIS-REP-001` 並需 ADR。

## 發現

無 blocking 或 high-severity finding。乾淨 checkout 首次執行需先建立 `.temp/pytest`（git-ignored 環境前置條件，與 G2/G3/G4 審查一致）。`binary_stl_semantic_fingerprint` 等 G1 既有函式在乾淨 clone 中可正常 import，無隱藏依賴主樹路徑。

## 狀態檔與 Git 一致性

- `execution/WORK_QUEUE.yaml`：currentWorkItem = `G5-REV-001`，G5 gate = `active`；G5 四個工項（PKG-001／BOM-001／DRW-001／REP-001）皆 `done`，G5-REV-001 為唯一 in_progress。
- 審查基準 commit：`51259f6430cee2b1ea4c390e79d268a4dc187696`（G5-REP-001 閉合 commit 之上）。

## 未登記的風險／假設

- G5-ACC-001（gate acceptance 決策紀錄）完成後 G5 gate 才能轉 accepted；本次審查不涵蓋 E4 簽核。
- G5-REP-001 的可重現契約以「鎖定工具鏈＋golden IR」為邊界；更換 OS／Python minor／CAD 版本需重新凍存 golden 並走 ADR 規則。
- 本機全綠，GitHub Actions 需另以 `origin` 分支重跑確認（無 pending failure）。
