# G0-CAD-001 evidence artifacts

此資料夾內容由 `python -m spikes.g0_cad.reference_case` 產生，只是 CadQuery feasibility spike 證據。

它們不是工程放行檔、不是完整電子治具設計，也不可直接用於製造。正式幾何契約、base、cover、assembly 與 downstream 驗證仍屬於 G1／G2 後續工作。
