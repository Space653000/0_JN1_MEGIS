# G0 Baseline CI 證據

`baseline-verification.json` 記錄 `scripts/run-baseline-ci.ps1` 在鎖定本機環境的最近一次完整結果。GitHub Actions 使用相同驗證入口；遠端 run 是否成功仍以 GitHub Actions 本身的 run 結果為準。

本證據只代表 baseline commands 與既有 feasibility artifacts 可重跑，不構成工程簽核或製造放行。
